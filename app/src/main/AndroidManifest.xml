<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.zing.zalo" />
    </queries>

    <application
        android:name=".ZpApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Zalo Patch"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZaloPatch">

        <provider
            android:name=".ConfigProvider"
            android:authorities="com.ez.zalopatch.config"
            android:exported="true" />

        <!-- LibXposed API 102 entry: META-INF/xposed/java_init.list declares
            com.ez.zalopatch.ZaloPatchModule, scope.list pins com.zing.zalo.
            The MODULE_SETTINGS shortcut is kept so the framework manager still
            opens this app's settings surface. -->
        <activity
            android:name=".StatusActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".DiagnosticCaptureTimeoutReceiver"
            android:exported="false" />

        <receiver
            android:name=".ZaloPackageReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SelfCheckReceiver"
            android:exported="true" />

        <receiver
            android:name=".CallRecordingImportReceiver"
            android:exported="true" />

        <receiver
            android:name=".CallRecordingNotificationReceiver"
            android:exported="true" />

        <service
            android:name=".PromoNotificationListenerService"
            android:exported="true"
            android:label="Zalo Patch promo notification filter"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".ZaloArtifactJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>
